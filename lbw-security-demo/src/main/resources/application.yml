spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/demo?useUnicode=yes&characterEncoding=utf8&useSSL=false

  session:
    store-type: none

# 2.0 默认日期返回不是时间戳
  jackson:
    serialization:
      write-dates-as-timestamps: true

